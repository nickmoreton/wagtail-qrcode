# setup testing mail environment
EMAIL_BACKEND = "django.core.mail.backends.smtp.EmailBackend"
EMAIL_HOST = "0.0.0.0"
EMAIL_PORT = 1025
EMAIL_HOST_USER = ""
EMAIL_HOST_PASSWORD = ""
EMAIL_USE_TLS = False

DEFAULT_FROM_EMAIL = "foo@localhost"
ALLOWED_HOSTS = ["*"]
WAGTAIL_QR_CODE_BASE_URL = "http://localhost:8000"

# needed when running on gitpod or testing with https://
CSRF_TRUSTED_ORIGINS = ['https://*.gitpod.io']